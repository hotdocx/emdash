# Bounded Homology: Proof-Field Comparison Review

Date: 2026-09-10
Baseline: `66af5e8ed4e06a68160eaa9678d0ccc727bdaeab`

This non-executable archive preserves the 20 follow-up source/driver
versions. No active library owner is changed by this checkpoint.
The [living plan](../../../docs/TYPESCRIPT_EMDASH_BOUNDED_LONG_EXACT_HOMOLOGY_AND_BOOK_PLAN.md#bounded-generator-and-endpoint-zero-continuation)
retains the complete goal and the next qualification boundary.

## Checked Construction And Remaining Consumer Boundary

The field-indexed ordinary Nat generator source-checks in four seconds in
`logs/probes/homology-row-fields-generator-quiet-20260910-145616.log`.
The state-based span facade passes the original 14 positive and one negative
controls, including whole transformation Hom action. Its generator wrapper
also source-checks.

The two-window value constructs and typechecks. With canonical PA terms,
the empty and one-window comparisons pass. Whole two-window comparison
still reaches 90 seconds, including the length-owner and named-recursion
variants. The named iterator source checks in 3.52 seconds but is not selected.
The arrow-observation probe passes its first two comparisons, then reaches
the bound before the remaining controls are qualified.

The relevant fresh logs are:

- `logs/probes/homology-row-fields-consumer-warnings-20260910-150354.log`;
- `logs/probes/homology-field-research-quiet-20260910-151348.log`.

The prefix `init` operation removes the final arrow and reuses property
evidence for the original penultimate object at the actual new endpoint.
Its four positive/one negative controls pass. It introduces no object
equality, cast, runtime rule or new homology choice.

## Proof-Field Hypothesis

The user suggested a constructor-level unifier comparing data and ignoring
proof payloads. This is an exploratory proposal, not a global path-collapse
policy. The concrete exact-step annotation has two suitable law fields:

- z is an equality in a Hom that the preadditive structure already proves
  set-valued;
- exact is IsEpic for the original selected boundary, defined by cancellation
  implications between Hom equalities, not by selected preimages.

The ambient selections, endpoints, arrows, kernels/cokernels and raw
computational coefficient witnesses remain data. Equality in an arbitrary
higher groupoid is not thereby declared proof-irrelevant.

The installed Lambdapi unifier decomposes identical constant/injective
heads before trying custom unification rules. The generic
`record_proof_irrelevance_unif_review.lp` control confirms:

- a constant introduction bypasses the proposed proof-discarding rule;
- an ordinary stable introduction permits the typed eq_refl comparison;
- raw runtime conversion still distinguishes the two proof payloads;
- distinct natural-number data are not identified.

The concrete owning-file copy changes the exact-step introduction from
constant to ordinary stable symbol, then adds a unifier retaining all
C/P/t/PA/A/B/D/f/g data indices and discarding only z/exact comparisons.
Its actual-record reviewer passes three positive and two negative controls:
typed eq_refl for distinct proof payloads, both original proof projections,
no runtime collapse, and rejection of an ill-typed exactness payload.
The combined source/reviewer call takes 64.17 seconds with normal subject
reduction. Its strict LHS audit is clean.

This establishes the local mechanism on the actual record layout, not its
integration into the main iterator. The modified record has its own probe
module path; it has not silently replaced the active constructor.

## Next Qualification

Prefer testing this local proof-field policy before adding an iterator head
merely to normalize complete proof-bearing values. Use typed eq_refl for the
proof-time comparison and retain independent runtime controls for actual
objects, arrows and sequence projections. Add explicit data/selection and
missing-evidence controls. Erasure during comparison must not manufacture
missing proofs or discard algorithmic witnesses.

The generic bounded endpoint application, conventional sequence ordering,
formal/native integration and final book revision remain required.

## Recovery

[manifest.json](manifest.json) records paths, statuses and hashes.
[prototypes.patch](prototypes.patch) recreates only ignored temporary sources.
Some prepared controls lie after a timeout and are explicitly unqualified;
the presence of an assertion is not evidence that it ran.

[verification.json](verification.json) records successful patch validation
and byte-for-byte/hash recovery of all 20 current prototype versions.

The later direct checks reused the fresh temporary research dependency tree.
No compiled objects or logs are archived, every Lambdapi call was bounded by
90 seconds, and normal subject reduction remained enabled. Final promotion
still requires a fresh exact-source owner/consumer gate.
