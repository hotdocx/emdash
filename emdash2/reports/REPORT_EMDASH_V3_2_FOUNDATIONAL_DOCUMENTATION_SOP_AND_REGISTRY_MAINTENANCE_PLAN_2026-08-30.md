# EMDASH v3.2 Foundational Documentation, SOP, And Registry Maintenance Plan

Date: 2026-08-30 (America/Toronto)

Plan-ID: `EMDASH-V3-2-FOUNDATIONAL-DOCS-SOP-REGISTRY-MAINTENANCE-2026-08-30`

Status: **complete; foundational authorities, lifecycle registry, and focused
anti-drift tooling synchronized without Lambdapi semantic changes**.

Branch: `goal/foundational-docs-maintenance-v3.2`

Worktree: `/home/user1/emdash1-foundational-maintenance-v3.2`

Baseline: `0e61a799b2cd0db0e61406a71d45d6b313f2c21e`, the clean integrated
`main` checkpoint containing the monad, selected Cartesian, pullback,
slice-dependent-product, property-profile, cubical, dependent-simplex, and
locally promoted `0.7.0-dev` book work.

Depends-On: root `AGENTS.md`; `emdash2/AGENTS.md`;
`EMDASH_FOUNDATIONS.md`;
`REPORT_EMDASH_V3_2_CANONICAL_SURFACE_SYNTAX_2026-06-05.md`;
`REPORT_EMDASH_V3_2_CURRENT_STATUS_AND_SOP_2026-05-26.md`; `INDEX.md`;
`REPORT_EMDASH_V3_2_AUTONOMOUS_MAINTENANCE_AND_EVOLUTION_PLAN_2026-07-22.md`;
`REPORT_EMDASH_CHECK_CATALOG.md`; `REPORT_EMDASH_HEALTH.md`; the active
Lambdapi sources and focused implementation plans for every statement being
summarized; and `../../docs/PERSISTENT_GOAL_GIT_EXPERIMENTATION.md`.

Supersedes: no mathematical or implementation plan. It reopens the completed
autonomous-maintenance lifecycle and authority-coherence findings under new
repository evidence, and supersedes only stale classifications or duplicated
standing prose corrected by this ledger.

Side-Task-Ledger: `FDM-00`, `FDM-BASE-1`, `FDM-ROLE-2`, `FDM-FOUND-3`,
`FDM-SYNTAX-4`, `FDM-AGENTS-5`, `FDM-STATUS-6`, `FDM-INDEX-7`,
`FDM-LINT-8`, `FDM-ROOT-9`, `FDM-REFS-10`, `FDM-CLEAN-11`,
`FDM-VALIDATE-12`, and `FDM-CLOSE-13`.

Infinity-Codex-Origin: session `01a02f68-6142-7e53-993a-4505aa8e2cbe`, response `0044_2026-08-30T11-24-42Z_01a05260-fe99-7141-ad53-075dc363d4b5.md`.
Infinity-Codex-Decision-Responses: `infinity-codex:01a02f68-6142-7e53-993a-4505aa8e2cbe:01a05260-fe99-7141-ad53-075dc363d4b5`

The user immediately approved that review and requested implementation as a
persistent goal.

## 1. Objective

Restore one coherent, low-drift authority architecture for the active emdash
v3.2 repository after the integration of several large mathematical and
editorial tranches.

The result must make these roles unambiguous:

```text
root AGENTS.md
  → repository, worktree, package, and cross-layer operating rules

emdash2/AGENTS.md
  → concise mandatory Lambdapi editing and validation SOP

CURRENT_STATUS_AND_SOP
  → current implementation architecture and owner map

EMDASH_FOUNDATIONS
  → coherent mathematical explanation

CANONICAL_SURFACE_SYNTAX
  → notation and executable-surface boundary

reports/INDEX.md
  → active/completed/deferred/superseded plan registry

CHECK_CATALOG + HEALTH
  → generated executable evidence.
```

The maintenance is primarily corrective and consolidating. It must add no
kernel rule, unification rule, theorem witness, public semantic owner, parser
feature, or TypeScript elaborator behavior merely to make the documents
simpler.

## 2. Authority And Recovery Order

On every continuation use:

1. active Lambdapi declarations, diagnostics, and focused examples;
2. root and nearest nested `AGENTS.md` instructions;
3. this living plan and its side-task/decision ledgers;
4. the current status report, Foundations, and canonical syntax according to
   their distinct roles above;
5. `reports/INDEX.md` for lifecycle and recovery routing;
6. generated catalog/health evidence;
7. dated implementation plans for historical decisions and measured warning
   evidence; and
8. archived responses only as recovery evidence.

When two standing documents disagree, active code decides the mathematical
fact. The maintenance then repairs the document that exceeded its assigned
role; it does not copy the disagreement into another authority.

## 3. Git And Scope Boundary

The user authorized:

- fast-forwarding the clean historical `main` worktree from `75a0b04` to the
  clean descendant `0e61a79`;
- the dedicated branch and worktree recorded above;
- implementation of this bounded maintenance architecture; and
- a persistent goal delegating its evolving details to this plan.

The completed fast-forward used `--ff-only`; it introduced no merge commit.

This authorization does not by itself include:

- push, pull request, tag, publication, release, or deployment;
- merge of this maintenance branch back to `main`;
- amend, rebase, reset, squash, force push, or other history rewriting;
- deletion of live branches or worktrees;
- importing or anticipating the unfinished
  `goal/global-strictness-profile-migration-v3.2` implementation; or
- local checkpoint commits unless the user separately authorizes them. The
  user's immediate 2026-08-30 follow-up now authorizes one final green local
  checkpoint for this completed tranche.

The maintenance describes the integrated `0e61a79` baseline. The global
strictness migration remains a later synchronization event. Standing prose
must continue to classify that migration as deferred and must not copy
in-flight rules, counts, or conclusions from its worktree.

## 4. Initial Evidence

### 4.1 Integration topology

Before fast-forward:

```text
main:                                             75a0b04
goal/emdash-book-categorical-structures-v3.2:    0e61a79
ahead-by:                                         6 commits
merge-base relation:                              main is an ancestor
both worktrees:                                   clean
```

The six-commit delta contained book sources, promoted book artifacts, the
book-expansion plan, report-map/current-status synchronization, and
`emdash2/tmp/EMAIL.md`. It changed no Lambdapi owner, diagnostic, example,
kernel script, or package configuration. `git diff --check main..HEAD`
passed.

### 4.2 Green structural baseline

At the identical `0e61a79` content before authoring this plan:

```text
source TOC:                 87 headings; sections 0–20; sequential
active-reference lint:      passed
report-header lint:         structurally passed for 27 entries
strict check catalog:       up to date
health source snapshot:     current
registered health files:    328
health snapshot SHA-256:    612715e4cbae9d012908bf1a6c344df3ea2e878f4a471bf69341f3ff3b4a7253
```

These gates show that paths, headers, generated evidence, and source
registration are mechanically healthy. They do not show that a plan called
“current” is semantically active, or that each conceptual authority covers
every newly integrated capability.

### 4.3 Concrete drift findings

1. `emdash2/AGENTS.md` is 1,963 lines. Its opening module catalogue and
   numbered authority inventory duplicate much of the current-status report.
   The opening catalogue includes slice dependent products, while the
   numbered inventory jumps from pullbacks at 127 directly to cubical work at
   128. The two standing representations already disagree.
2. `EMDASH_FOUNDATIONS.md` explains monads, pullbacks,
   `Σ_u ⊣ u* ⊣ Π_u`, and the derived cubical construction, but contains no
   selected `BinaryProducts`, `TerminalObject`, or `CartesianCategory`
   development.
3. The canonical-syntax report contains monad, pullback, dependent-product,
   and cubical notation but no selected binary-product, terminal-object, or
   thin Cartesian-package notation.
4. The current-status report is substantially current through the newest
   kernel layers and the `0.7.0-dev` book, but still says “Last consolidated:
   2026-08-28” and duplicates a large amount of operational SOP and historical
   chronology.
5. `reports/INDEX.md` is dated 2026-08-26. Its `Current Plans` section contains
   at least fourteen completed kernel plans plus two implemented external
   plans. An older profiled-Gray entry still describes the retired primitive
   strict-code/decoder design even though the later property-profile plan
   supersedes it.
6. `lint_report_headers.py` checks only file existence and required fields.
   It reports the 27-entry section green even when explicitly completed plans
   are classified as current.
7. `git worktree prune --dry-run --verbose` finds one dead temporary metadata
   entry: `/tmp/emdash-gray-graph-owner-probe-20260825`. No live historical
   worktree has been selected for removal.

## 5. Governing Decisions

### D-FDM-001 — one role per standing authority

Accepted. Operational commands and safety rules belong in discovered
`AGENTS.md`; current implementation descriptions belong in the current-status
report; mathematics belongs in Foundations; notation belongs in canonical
syntax; chronological experiment evidence belongs in dated plans.

### D-FDM-002 — preserve detail, remove standing duplication

Accepted. Consolidation must not delete unique warning classifications,
nonclaims, or recovery evidence. Unique dated evidence moves or remains in a
dated plan/report. Repeated exhaustive module prose should not remain in both
`emdash2/AGENTS.md` and the current-status report.

### D-FDM-003 — current registry means lifecycle-current

Accepted. The plan registry must distinguish active work from completed
current-architecture ledgers, deferred proposals, superseded history, audits,
and generated reports. “Current” may not silently mean “recently useful even
though complete.”

### D-FDM-004 — no semantic completion by documentation

Accepted. Missing Beck–Chevalley, Frobenius, slice exponentials, global
strict-cut migration, cubical degeneracies/connections/Kan filling, free-monad
syntax, automatic weighted-product witnesses, and whole equivalence packages
remain exactly as active sources and completed plans state.

### D-FDM-005 — generated evidence remains generated

Accepted. `REPORT_EMDASH_CHECK_CATALOG.md`, `REPORT_EMDASH_HEALTH.md`, assembled
book Markdown, and PDFs are never hand-edited. Their owning checks decide
freshness. Documentation-only edits do not force an expensive health refresh
when the canonical source-metrics payload is unchanged.

### D-FDM-006 — prevent the observed registry recurrence

Accepted. Registry cleanup is incomplete without strengthening the linter and
adding focused tests that reject completed status in the active section,
duplicate category registration, and missing/superseded active files.

### D-FDM-007 — no-churn is a valid audit result

Accepted. Root `AGENTS.md`, README files, scripts, and reports that already
serve their assigned role remain unchanged unless a concrete contradiction,
duplication, broken route, or missing invariant is found.

## 6. Intended Authority Architecture

### 6.1 Root `AGENTS.md`

Retain repository/workspace routing, TypeScript trust boundaries, persistent
goal/Git workflow, commands, and cross-layer validation policy. Do not add a
Lambdapi module list or summarize recent mathematics here. Add at most a
compact link or authority-role clarification if the nested cleanup makes one
necessary.

### 6.2 `emdash2/AGENTS.md`

Retain the mandatory material beginning with task startup, bounded checks,
rewrite/unification hygiene, higher-owner discipline, comments/layout,
MathOps validation, general conventions, long-running experiments, local
references, and recovery.

Replace duplicated exhaustive module catalogues with:

- a concise mathematical scope statement;
- a short authority order;
- a compact list of foundational owner families whose distinction affects
  rule design; and
- direct links to the current-status owner map, Foundations, canonical
  syntax, report index, catalog, and health report.

Before removing any paragraph, classify it as:

```text
operational invariant          → retain in AGENTS
current owner/status           → current-status report
mathematical explanation       → Foundations
surface notation               → canonical syntax
dated warning/probe evidence   → implementation plan
duplicate                      → remove after route verification.
```

### 6.3 Foundations

Add one coherent selected-Cartesian section near the universal-construction
sequence. It must explain:

- `BinaryProducts(C,P)` indexed by one whole `P:C×C→C`;
- whole projection transfors and represented-family pairing;
- stable `K₁ᵃ`/`K₂ᵃ`, beta, distribution, eta, and selected Došen
  normalizations;
- proof-time `P[f,g]` comparison with the triangular map;
- direct versus weighted-product presentations;
- `TerminalObject(C,t)`, whole `!:id_C⇒Const_t`, canonical terminal cut,
  Hom contractibility recentered at `!_A`, and derived uniqueness;
- the thin rule-free `CartesianCategory` package; and
- the explicit free-syntax/global-decision/automatic-weighted-witness
  nonclaims.

Retain the existing strong cubical explanation. Audit it only for stale
strict-code wording and align its central derivation with:

```text
ordinary Sigma + pointwise opposite + homd_int
  → homdc_int
  → homdc_total_cat
  → LaxArrow_cat
  → CubicalArrow_cat.
```

### 6.4 Canonical surface syntax

Add notation and equality-mode guidance for:

```text
P : C×C → C
κ₁, κ₂
K₁ᵃ(h), K₂ᵃ(h)
⟨f,g⟩
P[f,g] ≐ ⟨K₁ᵃ(f),K₂ᵃ(g)⟩
! : id_C ⇒ Const_t
!_B ∘ h → !_A
f = !_A
CartesianCategory(C,P,t).
```

State which lines are runtime, proof-time, or propositional. Preserve the
distinction from product categories, `BinaryProductPresentation`, and an
automatic weighted `DefIso`. Do not imply that the TypeScript parser supports
this full notation.

### 6.5 Current status report

Update the consolidation date and exact integrated book/repository state.
Keep a single current owner map and the architecture/invariant sections. Audit
the historical checkpoint appendix and duplicated SOP text with inbound-link
search before moving or removing anything. If extracted, preserve it verbatim
under a clearly historical dated report registered by `INDEX.md`.

Execution rules discovered through `emdash2/AGENTS.md` must outrank any
remaining duplicated prose in this report. The filename may remain stable to
avoid gratuitous link migration.

### 6.6 Reports index and lifecycle checker

Use explicit lifecycle categories:

```text
Active plans
Completed current-architecture ledgers
Deferred proposals
Superseded or historical records
Audits and retirements
Generated or semi-generated reports.
```

The exact heading names may preserve compatibility with existing scripts, but
their semantics must be unambiguous. Reclassify every current entry from its
own header and current supersession evidence. Correct stale summaries rather
than rewriting completed plan bodies as though their historical terminology
had never existed.

Strengthen `lint_report_headers.py` and add focused tests for:

- active entry with completed/implemented status;
- valid active status;
- completed-ledger entry with completed status;
- duplicate file across lifecycle categories;
- missing file;
- missing required provenance header;
- superseded plan incorrectly active; and
- live repository registry success.

## 7. Ancillary Audit

Audit, without novelty edits:

- root and `emdash2` READMEs;
- source-registration arrays in `scripts/check.sh` and
  `scripts/check_metrics.py`;
- `Makefile` commands and root wrapper routes;
- book/evidence links to standing authorities;
- recovery links and current plan pointers; and
- stale worktree metadata.

Do not change source registration when all 328 files and the generated
snapshot are already current. Do not remove a live historical worktree. The
one dead temporary metadata entry may be pruned only after the same exact
dry-run result is reconfirmed.

## 8. Validation Policy

This goal does not change Lambdapi semantics. Use proportional validation:

### Every documentation tranche

```bash
git diff --check
python3 scripts/check_source_toc.py
./scripts/lint_active_refs.sh
python3 scripts/lint_report_headers.py
python3 scripts/generate_check_catalog.py --check --strict
python3 scripts/check_metrics.py --no-check --check-report --brief
```

### Registry-linter tranche

Also run:

```bash
python3 -m py_compile scripts/*.py
python3 -m unittest <focused report-registry tests>
```

Register a new focused test in `Makefile` CI only if the checker behavior
changes. Run shell syntax only when shell scripts change.

### Final boundary

- exact staged/unstaged review;
- every focused test and documentation gate above;
- book evidence/source checks only if book-owned sources or evidence routes
  change;
- no book render for non-pagination documentation;
- no warning summary, Lambdapi aggregate, `make health`, or repository-wide
  `check:all` unless executable sources/checks or another concrete boundary
  actually changes.

## 9. Side-Task Ledger

| ID | State | Deliverable |
| --- | --- | --- |
| `FDM-00` | complete | Verified clean descendant topology; fast-forwarded historical `main` from `75a0b04` to `0e61a79` with `--ff-only`; created and bootstrapped the dedicated branch/worktree. |
| `FDM-BASE-1` | complete read-only audit | Structural baseline is green: 87-heading TOC, active references, current catalog, and 328-file source-only health snapshot. Recorded concrete authority, registry, and lifecycle drift. |
| `FDM-ROLE-2` | complete | Established and registered this plan, fixed its recovery header, activated the persistent goal, and made the one-role-per-authority contract explicit before consolidation. Infinity Codex verification passes in the historical root archive with 880 responses. |
| `FDM-FOUND-3` | complete | Added one coherent selected binary/empty-product section between monads and pullbacks: whole product/projection/pairing, all triangular cuts, the single proof-time map comparison, pointwise whole-pair/unpair boundary, Hom-contractible terminality, thin Cartesian packaging, weighted adapter, and exact nonclaims. The existing derived cubical/property sections contain no retired strict-code wording and remain authoritative. |
| `FDM-SYNTAX-4` | complete | Added canonical `P`, `κ₁/κ₂`, `K₁ᵃ/K₂ᵃ`, pairing, unpair, terminal-arrow, Cartesian-package, and weighted-adapter notation with explicit runtime, proof-time, and internal-equality modes; updated the review date and preserved the TypeScript parser boundary. Active-reference and report-header lints plus diff hygiene pass. |
| `FDM-AGENTS-5` | complete | Reduced nested guidance from 1,963 to 376 lines by replacing two competing exhaustive module inventories with a compact role/owner map. Preserved the mandatory operational tail byte-for-byte except for routing “current SOP” to current status and making full CI proportional to semantic edits. Added the missing selected Cartesian/pullback/`Π_u` and derived-cubical/property boundaries plus the explicit in-flight strictness exclusion. TOC, reference, header, and diff gates pass. |
| `FDM-STATUS-6` | complete | Updated the consolidation date, authority precedence, active child maintenance route, Gray property architecture, pullback-versus-general-`Π` boundary, exact 2,359-check/116-area catalog, fresh 1,117/157 warning measurement, zero/63/38 strict audit, 328-file health snapshots, and promoted `0.7.0-dev` book evidence. Retained the already fenced historical appendix in place: extraction would create large recovery/link churn without correcting a current-authority contradiction. Reference/header/diff gates pass. |
| `FDM-INDEX-7` | complete | Preserved and mechanically regrouped all 28 detailed registry summaries into 9 active, 17 completed-current, 1 deferred, and 1 superseded ledger; updated the index date and maintenance rules. Corrected the completed Gray summary to distinguish its historical code design from the integrated property package and later groupoidification/book work. No plan body or recovery summary was dropped. |
| `FDM-LINT-8` | complete | Replaced the field-only current-plan check with lifecycle/header validation for all four categories, wrapped headers, duplicate registration, file existence, required provenance, and status/category agreement. Added eight focused tests, including the exact completed-under-active recurrence, and registered them in CI. The live registry and all 52 relevant Python tests pass. |
| `FDM-ROOT-9` | complete no-churn audit | Root `AGENTS.md` already owns only repository/worktree/package and cross-layer routing, correctly delegates Lambdapi work to nested guidance, and needs no mathematical catalogue or edit. |
| `FDM-REFS-10` | complete | Confirmed source-registration arrays include all monad/Cartesian/pullback/`Π_u` and cubical owners and the 328-file health snapshot is fresh. Corrected only concrete README drift: added the integrated universal-construction chain, routed mandatory workflow to nested `AGENTS.md`, and advanced the local book label from `0.6.1-dev`/355 to `0.7.0-dev`/374 while distinguishing the latest public DOI. Wrapper, book/evidence, handoff, and recovery routes otherwise remain unchanged. |
| `FDM-CLEAN-11` | complete | Reconfirmed the dry run, pruned only dead metadata for `/tmp/emdash-gray-graph-owner-probe-20260825`, and reran the dry run with no remaining candidate. No branch, file, or live worktree was removed. |
| `FDM-VALIDATE-12` | complete | Final proportional gates pass: 87-heading source TOC, active references, lifecycle/header registry (`active=8`, `completed=18`, `deferred=1`, `superseded=1` after closeout reclassification), strict 2,359-check/116-area catalog freshness, 328-file health snapshot `612715e4…`, all 52 relevant Python tests, Python compilation, and diff hygiene. The fresh warning audit is `1,117/157`; strict LHS audit is zero/63/38. No Lambdapi or render aggregate was run because no executable mathematical or pagination-owned source changed. |
| `FDM-CLOSE-13` | complete | Synchronized this ledger and current-status/index routes. Historical `main` remains at integrated `0e61a79`; the user separately authorized one final local checkpoint after reviewing the completed goal, and this ledger is included in that exact checkpoint tranche. The in-progress global strictness migration remains an explicit later documentation-sync boundary. |

## 10. Acceptance And Stop Conditions

The goal is complete only when:

- each standing authority has one explicit role and no unresolved
  contradiction with another standing authority;
- selected products, terminality, and the thin Cartesian package appear in
  Foundations and canonical syntax at their exact active strength;
- the triangular origin of the cubical square remains explicit;
- `emdash2/AGENTS.md` contains every mandatory operational invariant but no
  competing exhaustive current-status catalogue;
- current status and report index agree about integrated owners and plan
  lifecycle;
- the registry checker rejects the observed completed-under-active defect;
- generated catalog/health evidence remains fresh without hand edits;
- root guidance and ancillary routes are either corrected or recorded as
  no-churn audits;
- only dead temporary worktree metadata is pruned;
- all proportional gates pass; and
- the ongoing global strictness migration remains outside this branch and is
  named as a required later documentation synchronization.

Stop and request direction rather than:

- choosing a new mathematical normal form;
- deleting unique historical evidence;
- changing a public implementation claim unsupported by active code;
- importing in-flight strictness-migration behavior;
- running a broad aggregate merely for reassurance; or
- committing, merging, publishing, or deleting live worktrees without the
  corresponding authorization.

## 11. Persistent-Goal Launch Prompt

> Continue the foundational documentation, SOP, and registry maintenance in
> `/home/user1/emdash1-foundational-maintenance-v3.2` on branch
> `goal/foundational-docs-maintenance-v3.2`, delegating exact authority roles,
> lifecycle taxonomy, source-backed corrections, consolidation boundaries,
> sequencing, tests, validation, Git discipline, and stop conditions to
> `emdash2/reports/REPORT_EMDASH_V3_2_FOUNDATIONAL_DOCUMENTATION_SOP_AND_REGISTRY_MAINTENANCE_PLAN_2026-08-30.md`.
> Begin from integrated baseline `0e61a79`. Preserve active Lambdapi semantics
> and generated artifacts; add missing selected Cartesian mathematics and
> notation; consolidate discovered SOP versus status duplication without
> losing unique invariants; repair plan lifecycle classification and prevent
> recurrence with focused tooling; audit root/README/script routes
> no-churn-first; prune only confirmed dead temporary worktree metadata; and
> keep the in-progress global strictness migration outside this branch.
> Follow proportional documentation/tooling validation. Branch/worktree
> creation and implementation are authorized; local checkpoint commits,
> merge, push, publication, history rewriting, and live-worktree cleanup are
> not authorized unless separately requested.
