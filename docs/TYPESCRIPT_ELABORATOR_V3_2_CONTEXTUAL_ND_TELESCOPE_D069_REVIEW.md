# D-DTTLF-USABILITY-069 — Contextual `:^nd` Telescope Text-Parity Review

Date: 2026-08-02

Gate: H-DTTLF-USABILITY-CONTEXTUAL-ND-TELESCOPE-TEXT-PARITY-01

Decision: D-DTTLF-USABILITY-069

Status: approved as proposed under the user's standing unattended delegation

Human-Supersession: any later explicit human decision supersedes this record

Reviewed-Proposal-Checkpoint:
`8012405ce3bc4d14a0bdd267ab70ee81cd5f02c2`
(`docs(v3.2): freeze contextual nd telescope text parity`)

## Review

No immediate human objection followed the immutable twelve-item proposal in
[`TYPESCRIPT_ELABORATOR_V3_2_CONTEXTUAL_ND_TELESCOPE_PLAN.md`](./TYPESCRIPT_ELABORATOR_V3_2_CONTEXTUAL_ND_TELESCOPE_PLAN.md).
The user's standing unattended delegation therefore approves it with immediate
human supersession.

The proposal closes a measured resolver seam rather than adding semantics.
Grouped comma/semicolon syntax, displayed-family annotation checking,
callback-local term resolution, and canonical layer validation already serve
`^fd`. The final-green typed method at
`01848adf70acbb49e2f6dbbe35b8fef90b517915` already owns endpoint synthesis,
factorization, internal action, and failure. The new text expected contract
therefore supplies only ordered source groups; it must not duplicate endpoint
functors or admit pointwise naturality evidence.

The review confirms that the same grouped validation and presentation logic
should be shared by `^fd` and `^nd`, while the existing recursive body resolver
continues to own eta, identity, composition, pairing, and fixed-head
application. The implementation must remain classifier-head-agnostic. Prior
alternating `Hom_catd`/`Transf_catd`, fixed mixed-target, and `homd_int`
evidence is sufficient; unrestricted variance remains a non-claim rather than
a prerequisite invented for this route.

## Exact Authorization

Implementation may perform only the twelve numbered D-069 items. It may add
one grouped displayed-transformation expected variant and resolver route,
reuse the existing grouped helper logic, generalize the post-elaboration layer
check to the new evidence variant, update truthful parity-audit prose, advance
the text revision exactly once, synchronize its nine existing pins, and add
the focused direct/text and fail-closed cases to the registered generic
telescope corpus.

Any production or test file outside the proposal's exact list requires a
separate frozen correction. Callback variables remain ordinary internal Core
terms owned by the typed method; no closure or evidence may survive
elaboration.

## Explicit Non-Authorization

This decision authorizes no grammar or located-AST change, second parser,
second elaborator, Core/checker/runtime case, Lambdapi declaration or rule,
external coherence payload, classifier-head decomposition, curry, product
facade, arbitrary dependency DAG, unrestricted variance or `:^nd`, arbitrary
point-arrow synthesis, browser/public/book behavior, package/barrel/runner
change, aggregate rerun, push, merge, publication, cleanup, or worktree
removal.

## Proportional Validation

Run typecheck, lint, whitespace hygiene, the focused parity-audit test, one
name-filtered generic-telescope text run, and an exact search proving the nine
revision pins. Carry forward the final-green typed semantic and kernel
evidence. Do not repeat the long aggregate, full telescope corpus, browser,
kernel, print, or book gates.
