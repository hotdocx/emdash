# Bounded Homology: Constructor-Case Iteration

Date: 2026-09-10
Baseline: `b15bad4272c99ed78035d1c49ef613a7214481c0`

This archive preserves 36 research source/driver versions. The
[living plan](../../../docs/TYPESCRIPT_EMDASH_BOUNDED_LONG_EXACT_HOMOLOGY_AND_BOOK_PLAN.md#bounded-generator-and-endpoint-zero-continuation)
retains the complete bounded theorem, endpoint, formal/native and book scope.
This tranche implements the iterator, not that entire completion boundary.

## What Changed The Next Action

The earlier field-indexed Nat iterator typechecked, but its full two-window
and several arrow comparisons reached the 90-second bound. A local exact-step
proof-field unifier passed its own positive and negative controls, including
missing/invalid evidence and distinct-arrow rejection. It did not resolve
the integrated runtime comparisons. That independently valid usability
experiment is therefore not installed in the active exact-step owner.

The scalar audit of the old iterator completed positions 0, 1, 3 and 6,
while 2, 4 and 5 reached the bound. Its trace exposes delayed next-row,
map and chain-zero projections inside the expanded connecting construction.
The new field-span case operation exposes the constructor fields BEFORE
calling the same semantic continuation. It adds one data-elimination beta,
not another homology operation, primitive Nat recursor or exactness law.

The fresh quiet and warning graphs with the ORIGINAL exact-step owner pass:

- `logs/probes/homology-case-owner-quiet-20260910-171734.log`;
- `logs/probes/homology-case-owner-warnings-20260910-172435.log`.

The five core controls check empty/one/two-window computation, the six-interior
result type, and literal retention of the second window after three rests.
The complete seven-arrow comparison and both length/overlap negatives pass.
Each reviewer takes four to five seconds in these runs. The warning
inventory matches the prior 1,249/169 boundary in every measured field.
Final source-path qualification is recorded in the parent plan.

Both final `check_homology_bounded_generator.sh` runs complete:
`homology-bounded-generator-warnings-20260910-173329.log` and
`homology-bounded-generator-quiet-20260910-174228.log` under
`logs/probes/`. They include 19 new positive and five negative controls
and all affected earlier window/span consumers. The final
[warning comparison](warning-comparison.json) preserves every inventory
field. The archive contains prototypes; the active owning modules contain
the qualified implementation.

## Packed-State Wrapper Remains Unqualified

Lambdapi's installed `handle/query.ml` prints
`assertion: it is true ...` BEFORE running the check. A displayed assertion
line is not evidence of completion. The combined and positive-only reviewers
reach their sixth assertion, the public-state-wrapper result comparison,
but do not complete it. Subsequent declarations establish that the preceding
five assertions completed; their separate final-source reviewer is green.

The isolated wrapper trace reaches unification of the initial H(inclusion)
index, whose row arguments are still projections of the packed state.
It is not evidence of a failed mathematical equality, a serialization problem
or a need to erase exactness proofs. The ordinary dependent triple eliminator
itself checks, but its explicitly typed wrapper and a specialized constructor
beta wrapper each reach the source-check bound. Neither is promoted.

The existing packed-state records are left intact. The field-indexed entry is
the canonical implementation entry. The packed-state agreement is now
consumer-gated, not a prerequisite for the bounded theorem: current
proof–CAS consumers do not use it. A consumer mixing the two presentations
could encounter this LF type-comparison cost, so it is a real usability
limitation rather than a mathematical failure or only a proof-certificate
question. Conventional bounded ordering and endpoint evidence remain required.

## Boundary Preparation And Invariants

The finite-arrow observer returns complete arrows in the existing LaxArrow
object carrier; it is a data view, not a replacement exact result.
The separately retained init prototype drops only the last arrow and final
annotation, and reuses endpoint-property evidence without object casts.
The short-exact-row-zero prototypes derive zero identity at both original
outer terms from zero in the middle. They do not assume zero homology.

The active implementation keeps the same whole row functors/transformations,
H, delta, kernel/cokernel selections, adjacent-zero proofs and exactness
witnesses. No global composition cut, opaque proof, record eta, object
transport, proof-field erasure or new mathematical premise is added.

## Recovery And Validation

[manifest.json](manifest.json) records every source path, size, SHA-256 and
qualification boundary. [prototypes.patch](prototypes.patch) recreates the
ignored research sources in an empty recovery directory; do not apply it
over an occupied worktree. Dependencies from earlier audits and the named
baseline remain necessary for replay.

[verification.json](verification.json) records patch validation and exact
byte/hash recovery. No compiled objects or large debug logs are archived.
All LF invocations use normal subject reduction and a 90-second per-target
ceiling. The research stages are temporary source-matched evidence, not a
permanent object cache or a substitute for final-source checking.
