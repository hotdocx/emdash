# Bounded Homology: Endpoint Construction And Conversion Isolation

Date: 2026-09-10
Baseline: `ff139362be71dfec03142d05dda2dc123b3624a5`

This non-executable archive preserves 55 research source/driver versions.
Preservation does not imply that a source or assertion passed. The
[living plan](../../../docs/TYPESCRIPT_EMDASH_BOUNDED_LONG_EXACT_HOMOLOGY_AND_BOOK_PLAN.md#bounded-generator-and-endpoint-zero-continuation)
retains the full generic bounded theorem, formal/native and book obligations.
No active LP owner changes in this research tranche.

## Checked Construction

The fresh graph in
`logs/probes/homology-boundary-research-quiet-20260910-175019.log` checks:

- generic boundary trimming, removing two leading arrows and the final
  arrow while retaining the middle arrows and their original annotations;
- its four positive/one negative controls;
- conventional ordering for L+2 windows; and
- the real two-window comparison and its four displayed arrows.

The generic dependent positive-span case and original last-middle-row
observation source-check. The generic final-source zero theorem checks in
42 seconds, and its two-window endpoint/last-row consumer in 13 seconds.
It proves zero identity on the SAME final H(A), from zero in the original
outside-support middle row. The generic first-target zero theorem checks
in 12 seconds, and the conventionally ordered left-endpoint specialization
in 14 seconds.

Generic trimming transfers any property of the original penultimate object
to the actual clipped target. Its preservation/noncollapse controls pass.
The separate zero-property specialization also source-checks. None of these
proofs replaces an endpoint object, assumes zero homology, or changes H,
delta, the universal selections or the original exactness witnesses.

## Corrected Owner And Fixture Mistakes

The first combined endpoint fixture omitted finite-tail-append; that source
is preserved as `homology_row_fields_endpoint_zero_missing_import.lp`.
It is a fixture defect, not mathematical evidence.

The first H(C)-zero helper used the equivalent generic target chain pair.
The existing window instead retains `homology_connecting_step_right_pair`.
Using that ACTUAL selected presentation makes both endpoint controls pass
in seven seconds. The earlier comparison is not repaired by an object cast
or an additional equality assumption. Both source versions are preserved.

The initial bounded-result-carrier probe guessed the nonexistent
`Sigma_grpd` name. The corrected `@Σ_` version source-checks. It is a
thin record prototype, not an instantiated fully checked bounded theorem.

## Unqualified Right-Endpoint Attachment

The generic untrimmed endpoint theorem and generic trimming property check
separately, but their homology specialization has not been qualified.
Annotated, inferred-result, explicit-input, zero-specialized, literal-endpoint,
two-case and separately typed-cut variants reach the 90-second bound.
The preserved statement is a separate theorem declaration: this is not
intentionally checking that two proofs normalize to the same proof.

The original trace repeatedly compares expanded endpoint recursors on a
named sequence and its trim/generator implementation. It records 364
unsuccessful unification-rule attempts and no successful unification-rule
application on those constraints. The large debug log is NOT archived.
This locates the difficult type conversion, not a proven LF implementation
bug or a demonstrated missing mathematical rule.

The stable-final-target experiment uses the same zero/successor recursion
at the original owning path. Its source, append/init, sequence and original
endpoint consumers pass, but the decisive right-endpoint attachment still
times out. It is NOT promoted and does not justify an injectivity assumption.

## Minimal Controls

- Generic annotations, Unit annotations and a zero-identity property with
  bare symbolic indices all pass in under one second.
- The actual symbolic threefold-length arithmetic and a preadditive
  structure obtained from an Abelian package pass in 20–21 seconds.
- The exact-step annotation with bare object/arrow indices passes in eight
  seconds.
- The complete homology endpoint statement checks separately in 15 seconds;
  the untrimmed theorem specializes separately to L+2 windows in 12 seconds.
- Taking an already-proved endpoint law as a parameter still times out.
- An OPAQUE tail at the actual H initial indices times out, even with only
  Unit annotations. No sequence or exactness-proof payload is constructed
  in that control.
- Keeping the same H objects but making the initial arrow an opaque typed
  parameter completes in 65 seconds. This is a diagnostic control, NOT an
  implementation that asks the user to supply or replace the computed arrow.

These controls rule out the exactness proof payloads, or the body of the
particular endpoint proof, as necessary causes. They identify the computed
initial H object/arrow indices as a material cost factor. They do not yet
establish a complete remedy.

The equivalent `drop_two(init(xs))` clipping-order variant source-checks
and preserves the concrete displayed arrows. Its first endpoint-composition
run also times out. The sequential rerun confirms both outcomes: source
and displayed-arrow checks finish in four/six seconds, while
`homology_bounded_right_zero_init_first-leaf-20260910-194408.log`
reaches 90 seconds. This rerun avoids the initial parallel launch's
possible duplicate compilation of the shared new dependency.

## Recovery

[manifest.json](manifest.json) lists source paths, byte counts and SHA-256.
[prototypes.patch](prototypes.patch) recreates the ignored research files in
an EMPTY recovery directory. Earlier audits and the baseline provide reused
dependencies. Do not apply the patch over an occupied worktree.

[verification.json](verification.json) records byte/hash recovery.
The local Git attribute exempts only the raw recovery patch from whitespace
lint: it deliberately retains 17 whitespace-only prototype lines so the
recorded original byte counts and hashes remain recoverable. It does not
relax checks on active sources, tests, scripts or documentation.
No compiled objects or logs are committed. Every LF invocation retains
normal subject reduction and the 90-second per-target ceiling. The original
boundary stage was compared with all 516 active root LP sources, with no
mismatch; the stable-target stage deliberately substitutes only its named
owner. These are research source graphs, not permanent runtime caches.
