# D-DTTLF-PRODUCT-REVIEWER-002 — Active Export-Pin Correction Review

Date: 2026-07-30
Gate: H-DTTLF-PRODUCT-REVIEWER-CORRECTION-01
Decision: D-DTTLF-PRODUCT-REVIEWER-002
Status: approved exactly as proposed under the user's standing unattended
delegation
Human-Supersession: any later explicit human decision supersedes this record
Reviewed-Proposal-Checkpoint:
`81bf6d7788f0d3b478b18724b6e9a3755989882f`
(`docs: freeze active export pin correction`)

## Review

The proposal measures a deterministic evidence drift, not a semantic gap:
all 73 selected active-core commands still exist, retain their exact kind,
metadata, and text digest, and require only one current export-digest refresh,
48 exact ordinal refreshes, and the measured one-rule aggregate-count
correction. The user's response requested that this remain a narrow,
proportionate repair and that work return promptly to end-user features. No
human objection changed the exact proposed correction.

The standing unattended delegation therefore approves only the bounded
correction frozen at the checkpoint above, with human supersession.

## Authorized Scope

The implementation may:

- update current-authority core export pins from `18500…1b2` to `91f0…d3` in
  the nine exact active evidence files named by the proposal;
- update only the proposal's 48 measured command ordinals;
- advance only the eight affected core acquisition-contract revisions from
  suffix `-1` to suffix `-2`;
- update the active inventory from 620 to 621 rule commands and 656 to 657
  runtime clauses; and
- update the matching current-provenance test expectations.

It must preserve the old digest in the explicitly historical displayed-ND
audit and plan fields. It must retain every command text digest and metadata
check and pass the proposal's bounded validation before the integrated
reviewer checkpoint.

## Explicit Non-Authorization

This decision authorizes no Lambdapi source or semantic change, mathematical
owner/rule, Core/checker/evaluator/parser/browser behavior, dependency,
lockfile, deployment, publication, bulk-scale work, compatibility
infrastructure, or second acquisition path.

It authorizes no push, merge, PR, release, amend, rebase, reset, history
rewrite, cleanup, branch/worktree deletion, or publication.

After the green reviewer checkpoint, work returns directly to the selected
`SYNTAX-PARITY-0A` end-user capability inventory.
