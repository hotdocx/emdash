# Bounded Homology Assembly Research Checkpoint

Date: 2026-09-10

Baseline: `09f9dd1aece392be7deae0051f162d2dd025be94`

These non-executable patch artifacts preserve the bounded result-carrier
comparison. They are recovery evidence, not instructions to overwrite the
active kernel or a claim that every variant checks. The governing decision
is in the [living plan](../../../docs/TYPESCRIPT_EMDASH_BOUNDED_LONG_EXACT_HOMOLOGY_AND_BOOK_PLAN.md#finite-exact-result-tails-selected-indexed-record-design).

## Selected Result

Use the indexed record classifiers in the three library owners
`emdash3_2_finite_arrow_tails.lp`, `emdash3_2_finite_arrow_tail_append.lp`
and `emdash3_2_computational_exact_arrow_tails.lp`. Length and boundary
indices survive type reconstruction. The constructors require the same
arrow/annotation data as the transparent Nat/Sigma models; exactness is
supplied by the existing window proofs, not newly postulated. Six local
constructor/projection beta rules add no global cut or unifier.

The real two-window consumer checks with inferred indices, shares the same
H(i), and retains all six original exactness witnesses. The final-source
gate checks 17 positive and four negative assertions. Both the warning and
quiet staged gates pass, with each individual Lambdapi invocation bounded
by 90 seconds. The combined real reviewer takes seven seconds in both
final-source runs. This is not yet the general bounded row iterator or the
outside-support zero theorem.

## Preserved Alternatives And Controls

- [prototypes.patch](prototypes.patch) preserves the initial 24 source/driver
  versions: the alias/inferred endpoint comparison, transparent indexed
  tails, generic append, exactness annotations, explicit-index controls,
  direct prepending and projection-view experiments.
- [indexed-records.patch](indexed-records.patch) preserves 12 further
  versions: rigid tail/annotation classifiers, their controls and both
  successful real-consumer argument policies.
- [manifest.json](manifest.json) records exact paths, statuses and SHA-256
  hashes; [verification.json](verification.json) records byte-for-byte
  recovery. These patches create only the listed temporary probe paths.
- [warning-comparison.json](warning-comparison.json) records equality of
  every warning-inventory field at the final source boundary: 1,249
  critical-pair and 169 pattern reports, with no additional warnings.

The transparent model was not rejected mathematically. Its erased indices
required explicit lengths/endpoints; even then, generic append and later
projection comparisons reached 90 seconds. The fully explicit direct
prepending diagnostic printed a checked six-interior `joined` definition
before timing out in the subsequent observations. Merely adding transparent
exact-tail projection aliases did not fix their source-check cost.

The small scalar diagnostics require careful interpretation. Some fail
implicit reconstruction; later ones pass the direct pair/evidence/arrow
observations before timing out in nested proof projections. Their trailing
wrong-interior negative is not thereby an independently measured failure.
The registered reviewer instead has separately checked missing-evidence and
wrong-boundary controls. No broad unifier was added to force those older
diagnostics to pass.

The first `homology_adjacent_window_tail_prepend.lp` has a generator-clipping
syntax error. It is preserved as a fixture defect, not evidence against
prepending. The `_prepend_full` version fixes the clipping and reaches the
checked combined result described above. Future source transformations must
read the complete file and verify expected anchors before editing.

## Reproduction And Scope

From `emdash2`, run `scripts/check_homology_arrow_tails.sh`; set
`EMDASH_LAMBDAPI_WARNINGS=1` for the warning inventory. The gate uses a fresh
exact-source copy and checks each prerequisite/owner/reviewer separately.
It leaves no persistent object cache. The registered source/example/health
dispatch is covered by focused Python tests, including failure propagation.

No unrelated repository aggregate, whole-op repair, TypeScript core change,
new H selection, endpoint cast, or closed Freyd model construction belongs
to this checkpoint. All remaining bounded assembly and book obligations
remain in the parent goal.
