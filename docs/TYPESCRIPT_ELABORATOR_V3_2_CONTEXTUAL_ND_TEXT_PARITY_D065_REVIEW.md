# D-DTTLF-USABILITY-065 — Compact Contextual `:^nd` Text Review

Date: 2026-08-02

Gate: H-DTTLF-USABILITY-CONTEXTUAL-ND-TEXT-PARITY-01

Decision: D-DTTLF-USABILITY-065

Status: approved as proposed under the user's standing unattended delegation

Human-Supersession: any later explicit human decision supersedes this record

Reviewed-Proposal-Checkpoint:
`a4ee654d8e025df6962ea92f219819430852f51a`
(`docs(v3.2): freeze compact nd text parity`)

## Review

No immediate human objection followed checkpointing of the exact bounded
`CONTEXTUAL-ND-TEXT-PARITY-1AI` proposal in
[`TYPESCRIPT_ELABORATOR_V3_2_CONTEXTUAL_ND_TEXT_PARITY_PLAN.md`](./TYPESCRIPT_ELABORATOR_V3_2_CONTEXTUAL_ND_TEXT_PARITY_PLAN.md).
The user's standing unattended delegation therefore approves that proposal
with immediate human supersession.

The executable 0AH audit establishes that the direct compact contextual API
is already coherent and that the text delta is adapter-only:

- neutral application constructs contextual eta, prewhiskering, and
  postwhiskering;
- existing `composeCells` constructs recursive contextual vertical
  composition;
- existing `identityCell` constructs contextual identity;
- all five routes are accepted only after the direct factorer recovers a
  closed whole `Transfd`; and
- historical text `lambda^nd k : K. eta k` remains green through the existing
  base-component route.

Current compact text fails at exactly two resolver seams: a displayed-family
annotation reaches the historical category-annotation contract, and
`identityCell` is not a named fixed application head. No grammar, Core,
checker, runtime, or mathematical owner is missing.

## Exact Authorization

The implementation may only:

- add one additive `displayed-context-transfor` expected-contract kind;
- retain the existing source displayed family as the optional binder-
  annotation expectation and retain the two endpoint terms as semantic
  authority;
- select the compact route only from that expected kind and call the existing
  `CoreCategoricalProgram.displayedTransforContextLambda` method once;
- add one fixed one-argument `identityCell` resolver head;
- leave eta and both whiskers on neutral application and leave composition on
  existing `composeCells`;
- bump the categorical-text revision exactly once;
- extend the existing text-parity test file with direct/text equality for the
  five reviewed bodies, a fixed alternating Hom/Transf target, exact
  annotation/endpoint/orientation/point/expectation negatives, and unchanged
  historical base-component evidence; and
- synchronize the living plans and handoff.

## Required Invariants

The source-family field is a checked presentation contract for the optional
annotation. It must not cast, coerce, or override the endpoint classifier.
The existing program method remains responsible for checking that the source
and target displayed functors share their actual base, source family, and
target family. The direct point factorer remains the sole authority that can
turn parsed point syntax into a coherent whole `Transfd`.

The resolver must continue evaluating each callback exactly once through the
existing program method. It must not retain a callback, inspect classifier
expressions to invent an expected contract, or add any action/coherence case
for Hom- or Transf-headed targets.

## Explicit Non-Authorization

This decision authorizes no new token, located syntax node, grammar
production, parser dependency, categorical-program method, Core/LF case,
checker/conversion/evaluator/runtime/proof rule, Lambdapi declaration or
rewrite, `Transf_catd_func`, curry/section/cast route, external naturality
evidence, arbitrary point-arrow synthesis, unrestricted `:^nd`, browser
preset, public report, README/book change, scale row, package/workspace/root-
runner change, publication, merge, push, or wider Git mutation.

## Proportional Validation

The implementation must pass the affected existing text-parity file, nearest
direct contextual eta/identity/composition/whiskering tests, root TypeScript
typecheck and lint, whitespace, and exact staged review. It must not repeat the
long aggregate because the proposal changes none of its owning shared
boundaries; the recent qualified aggregate remains the applicable evidence.
