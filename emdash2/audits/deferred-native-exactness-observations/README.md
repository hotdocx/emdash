# Deferred Native Exactness Observation Experiments

Status: user-deferred on 2026-09-14; not a prerequisite for direct native model integration

Baseline: `0caa19d011dee802f2cfe94a7b163758907682eb`

Authority: [living model/reifier plan](../../../docs/TYPESCRIPT_EMDASH_NATIVE_MODEL_REIFIER_PLAN.md)

These text snapshots are recovery evidence, not active declarations or
registered positive examples. Their hashes are in `snapshots.json`. Restore
a selected snapshot as an ignored `tmp/probes/*.lp` only for an explicitly
selected later investigation or a demonstrated required native consumer.
Do not automatically resume the projection/checker detour.

Required native consumer update (2026-09-15, NUH-5N2G3A): typed point
observations now work through a Sigma of the original arrow and Ω evidence,
checked native evaluation constructors and typed projections. The frontend
emits checked transparent named results with their full bodies/dependency
closure. This qualifies the current native consumer without another
assumption, opacity rule or checker patch. Direct expanded point types and
inline concrete emission still exceed 2GiB. The inverse/projection studies
below and the combined δ-point/exactness import remain unqualified; do not
interpret this consumer result as their resolution. Displayed-diagram
exactness transport is still required in the living plan.

The original native whole exactness theorems remain qualified. The generic
evaluation lemma checks, as do the twelve whole-API prototype definitions.
The middle point-proof definition is accepted with its inferred type. Its
explicit point-predicate/inverse review exceeds the unchanged 2-GiB guard;
the combined whole-projection review also exceeds that guard. These are
resource failures, not established counterexamples. No opacity, axiom,
rewrite or checker patch was promoted to bypass them.

| Snapshot | Evidence |
| --- | --- |
| `generic_evaluation.lp.txt` | Passes three assertions, including both selected inverse projections; `logs/probes/nuh5b2e1_eval_equiv-20260914-225010.log` |
| `point_review.lp.txt` | Allocation failure; `logs/probes/nuh5b2e1_inferred_middle_review-20260914-231325.log`; earlier collection at `o=20` did not resolve it |
| `whole_api.lp.txt` | Twelve whole comparison/type/evidence definitions check; `logs/probes/nuh5b2e1_whole_api-20260914-233447.log`; useful as a prototype for the direct native route |
| `whole_type_review.lp.txt` | Three whole exactness constructor/predicate checks pass; `logs/probes/nuh5b2e1_whole_type_review-20260914-234202.log` |
| `whole_projection_review.lp.txt` | Combined type/inverse/Hom-action review exhausts allocation; `logs/probes/nuh5b2e1_whole_review-20260914-233002.log` |

The older endpoint issue is separate evidence. The
[previous bounded-homology plan](../../../docs/TYPESCRIPT_EMDASH_BOUNDED_LONG_EXACT_HOMOLOGY_AND_BOOK_PLAN.md)
records a symbolic zero-endpoint attachment failure reduced to an additive
law at native zero-cone projections, without H, exactness or an Abelian
package. The present probes concern ΩEquivAlong and native exactness
observations. Both involve projection/conversion, but a common cause has
not been established. Do not claim that resolving one resolves the other.

The primary continuation uses `FreydAdjunctionModel` and native whole P/Q,
H/maps/δ/exactness directly. Retain the CAS-selected results and coherent
realization contracts. Agreement with the older formal H/map/δ presentation
is later compatibility work. Required checks of the new native/CAS interface
remain mandatory; this deferral is not permission to assume output exactness.
