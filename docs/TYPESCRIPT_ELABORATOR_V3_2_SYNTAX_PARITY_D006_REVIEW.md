# D-DTTLF-PRODUCT-SYNTAX-PARITY-006 — Displayed Constructor Review

Date: 2026-07-30
Gate: H-DTTLF-PRODUCT-SYNTAX-PARITY-06
Decision: D-DTTLF-PRODUCT-SYNTAX-PARITY-006
Status: approved as proposed under the user's standing unattended delegation
Human-Supersession: any later explicit human decision supersedes this record
Reviewed-Proposal-Checkpoint:
`20093b71e3802c3d2b7cbf20670862a58a71b653`
(`docs(v3.2): freeze displayed constructor parity slice`)
Proposal-Ledger-Checkpoint:
`4b9dfc0ce43ac3fe888f29f80c568caf16f7beb8`
(`docs(v3.2): correct displayed audit checkpoint hash`)

## Review

No immediate human objection followed presentation and local checkpointing of
the bounded `SYNTAX-PARITY-1C2A` proposal in
[`TYPESCRIPT_ELABORATOR_V3_2_SYNTAX_PARITY_PLAN.md`](./TYPESCRIPT_ELABORATOR_V3_2_SYNTAX_PARITY_PLAN.md).
The user's standing unattended delegation therefore approves that exact
proposal with human supersession.

The selected sources are:

```text
pi1d B C
pi2d B C
paird FF GG
swapd B C
diagd B
sigmaProj E
pullbackFunctord FF F
sigmaPair E x u
transport E p
sigmaArrow E u v p alpha
pullbackTotal F E
composeTransfd theta eta
```

All twelve direct TypeScript methods are already checked and executable under
the reviewed descendant profile. Text currently fails only because these
displayed structural and comprehension operation heads are not reserved or
routed.

The review also accepts the audit correction:

- `eta x` and `eta x u` remain recursive generic whitespace applications;
- no redundant component or point alias is added;
- `eta p u` is not generic component application; and
- `displayedTransforNaturality` and the other three whole/higher internalized
  action constructors remain outside this decision as `SYNTAX-PARITY-1C2B`.

## Exact Authorization

The implementation may only:

- retain the existing three private located expression node kinds and parser
  grammar;
- recognize the exact twelve fixed-arity reserved application spines above;
- resolve every term operand recursively through the existing term resolver;
- resolve displayed-family operands only as checked immutable
  displayed-family bindings in this row;
- call the existing `displayedProductLeftProjection`,
  `displayedProductRightProjection`, `displayedProductPair`,
  `displayedProductSwap`, `displayedProductDiagonal`, `sigmaProjection`,
  `pullbackDisplayedFunctor`, `dependentPair`, `familyTransport`,
  `sigmaArrow`, `pullbackTotal`, and `composeDisplayedTransfor` methods;
- preserve generic `CoreCategoricalProgram.apply` as the only non-reserved
  application route;
- prove text/direct equality for backend-neutral explicit Core and rich
  inferred categorical classifiers for every selected constructor;
- preserve exact arity, kind, foreign-value, base/fibre, shared-source,
  transport, endpoint, and profile negatives; and
- synchronize only the syntax, handoff, and current product-route ledgers.

## Required Invariants

The implementation must:

- treat each selected head as presentation syntax for one existing typed
  program method, not as a new mathematical owner;
- delegate displayed bases/families, functor endpoints, Sigma fibres,
  transport boundaries, and profile availability to the direct program;
- preserve recursive term resolution and exact source spans;
- keep displayed-family identifiers immutable and foreign-family checked;
- avoid a constructor-owned action table, duplicated categorical checker, or
  fabricated Hom boundary;
- retain all existing binder, `indexOf`, `fibrePair`, `composeCells`,
  component/point, ordinary-constructor, and generic-application behavior;
  and
- fail closed outside the exact twelve-operation envelope.

## Explicit Non-Authorization

This decision authorizes no:

- `displayedFunctorFullAction`, `displayedFunctorInternalCell`,
  `displayedTransforNaturality`, or
  `displayedTransforInternalHomAction` text head;
- redundant alias for `displayedTransforComponent` or
  `displayedTransforPoint`;
- category or displayed-family result syntax, general dependent-family
  expression, or arbitrary-depth context syntax;
- new mathematical owner, categorical-program method, Core node,
  checker/evaluator branch, runtime/proof/unification rule, semantic profile,
  transfer input, or Lambdapi declaration/rule;
- expected-action table, parser dependency, exported raw-syntax layer, second
  resolver/checker, or alternate browser implementation;
- browser preset, book prose/artifact, README, scale row, deployment,
  publication, release, or repository-wide notation migration; or
- push, merge, PR, rebase, amend, reset, history rewrite, cleanup, branch
  deletion, or worktree removal.

## Validation And Git Boundary

The implementation must pass:

- exact text/direct equality for all twelve selected constructors;
- recursive nested-constructor positives where their direct classifiers
  compose;
- complete arity/kind/foreign/base/fibre/source/transport/endpoint/profile
  negatives;
- retained generic component and point equality plus non-generic naturality
  evidence;
- existing categorical text and syntax-parity regression corpora;
- root typecheck and lint;
- proportional browser checks only if shared browser code changes; and
- exact staged review plus `git diff --cached --check`.

It may then receive one bounded local implementation checkpoint and one
separate synchronized-ledger checkpoint under the existing Git authority.
