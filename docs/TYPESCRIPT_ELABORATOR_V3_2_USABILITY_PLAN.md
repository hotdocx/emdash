# TypeScript Categorical-Binder Usability For emdash v3.2 — Living Plan

Date: 2026-07-26
Plan-ID: TS-ELAB-V3.2-USABILITY
Depends-On:
[`TYPESCRIPT_ELABORATOR_V3_2_MASTER_PLAN.md`](./TYPESCRIPT_ELABORATOR_V3_2_MASTER_PLAN.md),
[`TYPESCRIPT_ELABORATOR_V3_2_DTT_LF_CONTINUATION_PLAN.md`](./TYPESCRIPT_ELABORATOR_V3_2_DTT_LF_CONTINUATION_PLAN.md),
[`TYPESCRIPT_ELABORATOR_V3_2_SCALE_QUALIFICATION_PLAN.md`](./TYPESCRIPT_ELABORATOR_V3_2_SCALE_QUALIFICATION_PLAN.md),
and the active emdash v3.2 mathematical authorities
Governs: the current end-user demo and categorical-binder frontend critical
path; it does not replace the completed-profile history or erase the separate
mechanical-transfer qualification ledger
Side-Task-Ledger: architecture decisions, implementation slices, experiments,
validation, and human-review gates in this file
Infinity-Codex-Origin:
`infinity-codex:019f9243-9fba-7c73-861b-ff4eacf0c56c:019f9fd9-637b-7af1-81be-a564a3129c5c`
Infinity-Codex-Recovery-Evidence:
`emdash2/tmp/ai-responses/sessions/2026-07-24_019f92439fba/responses/0028_2026-07-26T19-24-35Z_019f9fd9-637b-7af1-81be-a564a3129c5c.md`;
active code, authorities, and this living plan outrank the archived response
Human-Decision-Record: on 2026-07-26 the user agreed with the consolidated
review that canonical Lambdapi declaration parsing should be paused and
documented as optional/deferred, that the useful generic transfer
infrastructure should be preserved, that a polished already-supported
dependent demo should be completed first, and that the missing architectural
critical path is a TypeScript surface compiler for categorical binders and
type-directed applications. The user directed that the review be integrated
comprehensively into the plans, implementation continue, and a corresponding
persistent `/goal` objective be set.
Status: active living plan; D-DTTLF-USABILITY-001 is approved; USABILITY-0A
consolidation and USABILITY-0B's runnable dependent demo are complete;
USABILITY-1A is the next dependency-ready categorical-binder specification
slice, and wider declaration acquisition is explicitly deferred while the
binder frontend is qualified
USABILITY-0A/0B implementation checkpoint:
`c2c6da01386b7fda1d26e71379c4256fb06ea637`

## Outcome And Corrected Priority

No from-scratch backend redesign is indicated. The current backend-neutral
Core, generic outer LF, checker/evaluator, catalog-owned categorical
computation, generic transfer engines, and Lambdapi conformance boundary are
retained.

The highest-impact missing architecture is instead the frontend that turns an
ergonomic TypeScript expression with categorical binders and convenient
applications into explicit active emdash owners. The immediate work therefore
has two product-facing outcomes:

1. package the existing dependent Sigma-telescope computation as a runnable,
   readable TypeScript demo; and
2. implement and qualify categorical bracket abstraction and type-directed
   application, first for ordinary functors and then for one
   natural/displayed dependent example.

The demo does not depend on transcribing the measured remaining 70 root
commands or the two 58/25-command extension closures. Those transfers expand
library coverage and remain useful future scale evidence; they do not create
the first functioning dependent end-user path.

The final frontend design must not be called settled merely because the
backend accepts explicit Core. It becomes provisionally settled only after
the ordinary functorial slice and one natural/displayed Pi/Sigma slice pass
end to end without owner-specific checker or evaluator shortcuts.

## Authority And Trust Boundary

The authority order remains:

1. `../emdash2/emdash3_2.lp`;
2. the four active one-way extension modules named by `../emdash2/AGENTS.md`;
3. `../emdash2/emdash3_2_checks.lp`;
4. the current v3.2 status/SOP report;
5. `../emdash2/reports/EMDASH_FOUNDATIONS.md`;
6. the canonical-surface-syntax report;
7. the active task plans selected through `../emdash2/reports/INDEX.md`;
8. this usability plan for the frontend critical path;
9. the scale-qualification and DTT/LF continuation plans for their retained
   transfer and implementation evidence; and
10. the completed master plan and old TypeScript prototype as historical
    evidence only.

Handwritten Lambdapi remains the mathematical authority. The exact
`emdash-v3.2-mvp-1` browser profile and exact root-only
`emdash-v3.2-dttlf-directed-1` continuation keep their recorded boundaries.
This plan does not promote another profile, install another rule, broaden the
browser API, or authorize a kernel semantic change.

The intended product path remains:

```text
ergonomic TypeScript surface / typed macros
        (string parsing optional and deferred)
                         |
                         v
scope + expected types + variation/classifier constraints
                         |
                         v
small contextual/wiring IR for categorical abstraction
                         |
                         v
backend-neutral explicit emdash Core
              /                         \
             v                           v
small TypeScript checker/evaluator     deterministic Lambdapi emitter
             |                           |
             v                           v
bounded product execution             mandatory conformance oracle
```

No parser, generator, canonical export, or emitted Lambdapi file enters the
trusted semantic architecture. Canonical export remains checked derivative
evidence for inventory, source drift, selection, extraction, and differential
conformance.

## Current Functional Evidence

The implementation already has two genuine dependent layers:

- a minimal outer dependent LF with locally nameless λ/Π terms, an ergonomic
  one-shot scoped TypeScript builder, beta/delta computation, bidirectional
  checking, contextual metavariables, and retained variation metadata; and
- a reviewed directed/categorical-DTT continuation that transfers the
  foundational `Sigma_cat`/`Pi_cat` spine and related computation into the
  TypeScript checker/evaluator.

The exact reviewed continuation has 29 signatures and ten runtime rules. Its
existing end-to-end dependent witness constructs a Sigma telescope, forms a
dependent pair/fibre expression, infers and checks its type, performs outer
LF beta, and then fires
`directed.sigma-telescope-fibre.evaluate`. A wrong family/pair is rejected.
USABILITY-0B exposes that already executable evidence as a stable demo rather
than requiring readers to reconstruct it from a test.

The scale work additionally supplies one immutable transfer IR and generic
declaration, runtime, composed-runtime, proof-time-unification,
source-ordered mixed-phase, inductive-signature, intrinsic-delta, and exact
compiled-module-interface mechanisms. Representative J/Nat/Pi/Sigma,
dependent proof, internal Pi, Sigma-transfor, and profunctor tranches have
shown that those engines can absorb diverse data without owner-specific
evaluator cases.

That evidence settles neither all source coverage nor the end-user frontend:

- the current `CoreLfScopedBuilder.lam` is an outer-LF lambda;
- its variation mode is metadata and does not construct a categorical
  functor;
- the current surface has explicit operation nodes but no categorical
  bracket-abstraction compiler;
- the 29/10 continuation is much smaller than the full active development;
  and
- natural/displayed structural coverage is intentionally incomplete.

## Acquisition And Direct Transfer Decision

### Canonical declaration parsing is deferred

The paused experiment attempted only canonical `symbol` declarations and
explicitly excluded runtime rules, proof patterns, inductives, tactics, and
notation. It had already reached 1,022 source lines before dedicated tests and
was neither exported nor consumed. USABILITY-0A removes that isolated
untracked experiment.

The following completed work is independent and must be preserved:

- exact public/protected/private compiled module exposition;
- checked tactic-source retention as an opaque theorem boundary;
- exact source/export/import/hash acquisition contracts;
- measured source-prior closure and dependency inventory for
  SCALE-STRESS-3B; and
- the generic transfer engines through which a future direct or generated
  specification is compiled.

No architectural rule prohibits a future narrow parser or generator. It may
be reconsidered only after a measured repetitive transfer tranche shows that
such an adapter costs less than direct typed construction. Its output must
still feed the same IR and policy boundary, fail closed, remain reproducible,
and stay outside the trusted checker/evaluator.

### Direct typed transfer remains feasible and default

A Codex agent can construct the remaining typed transfer IR in bounded
tranches. A parser is not required. Per-command cost is nonuniform:

- an opaque signature is often roughly 10–40 lines plus linkage and tests;
- a straightforward transparent definition is often 20–100 lines;
- a runtime rule adds typed captures, patterns, subject-reduction evidence,
  positive cases, and near misses;
- a proof-time rule also adds variables, ordered constraints, and execution
  tests;
- an inductive adds generated owner/signature/computation boundaries; and
- a tactic theorem requires an explicit checked-source/opaque policy.

The measured “70 + 83 declarations” therefore is not 153 uniform headers.
The root prerequisite alone contains two inductives, many explicit theorem
bodies, two tactic bodies, protected visibility, and generated-owner
boundaries. Direct transfer remains feasible, but likely costs several
thousand lines plus review and differential testing.

For this project, “mechanical transfer” means that another already-qualified
mechanism is added through typed data, linkage, authority policy, and focused
evidence—not that every `.lp` term is automatically parsed. Small builder
helpers and command-specific checked extraction are encouraged when actual
repetition appears. A new checker/evaluator algorithm, owner-named shortcut,
or silently opaque unsupported term is not mechanical.

## The Outer-LF Versus Categorical-Binder Boundary

An outer LF abstraction such as an ordinary dependent function:

```text
λ x : A. t
```

is represented by `KernelLambda` and eliminated by ordinary LF application.
That remains correct and necessary.

A categorical abstraction:

```text
h = λ x :^f A. F[x]
```

has a different target. It must elaborate to an actual object
`h : Functor A B` (using the active source convention for that judgment), not
merely to an LF function from `τ(Obj A)`. In the eta case it can compile
directly to `F`.

The frontend may expose one convenient application form, but elaboration must
choose an explicit eliminator from classifier, argument dimension, expected
type, polarity, and displayed context. For the ordinary eta example:

```text
h[y]  ↦ fapp0 h y
      ↦ fapp0 F y
```

when `y : Obj A`, while:

```text
h[p]  ↦ fapp1_fapp0 h p
      ↦ fapp1_fapp0 F p
```

when `p : Hom A x y`.

The exact capped arrow-action owner is `fapp1_fapp0`.
`fapp1_func F x y` denotes the whole hom functor and is selected when the
expected result is that complete action rather than its value on one arrow.

The same application-classification family includes:

| Classifier and expected shape | Explicit target family |
| --- | --- |
| outer LF Π | ordinary Core call |
| functor, object argument | `fapp0` |
| functor, whole hom action expected | `fapp1_func` |
| functor, one arrow argument | `fapp1_fapp0` |
| transfor, point evaluator expected | `tapp0_func` |
| transfor, point value expected | `tapp0_fapp0` |
| transfor, full off-diagonal action expected | `tapp1_func` |
| transfor, one off-diagonal value expected | `tapp1_fapp0` |
| displayed functor/transfor | the corresponding active `fdapp*`/`tdapp*` owner |
| dependent section/Pi | `piapp0` or the corresponding active `piapp1_*` owner |

USABILITY-1A must turn this table into precise synthesis/checking judgments
against current owner signatures. Unsupported or ambiguous selection fails
with the inferred classifier, argument dimension, expected type, and candidate
owners; it never guesses.

## Contextual/Wiring IR And Bracket Abstraction

Active v3.2 already contains much of the ordinary categorical combinator basis
needed for an implementable compiler:

- product evaluation and higher action (`Eval_func`);
- product maps and pairing;
- curry and uncurry;
- functor composition;
- weakening through `Const_func_func`;
- exchange through `sym_func_func`; and
- contraction through `diag_func_func`.

The frontend will use a small first-order contextual/wiring IR between the
ergonomic surface and explicit Core. It records, at minimum:

- ordered categorical input slots and their classifiers;
- each subterm's free-slot usage;
- inferred result classifier and cell level;
- polarity/variance transformations;
- duplication, discard, and permutation requirements;
- application/evaluation edges; and
- source provenance for diagnostics.

A typed categorical bracket-abstraction pass then lowers:

- an unused variable to weakening/a constant functor;
- the variable itself to identity;
- reordered variables through symmetry;
- repeated variables through diagonal/contraction;
- an application through evaluation after typed pairing;
- composition through functor composition;
- a nested abstraction through curry; and
- multiple inputs through the active product/context construction.

A DAG or string-diagram representation may later improve sharing,
visualization, or diagnostics, but it is not required for the first slice.
Graph sharing is an implementation optimization; categorical contraction is
a semantic duplication of input and must remain explicit.

The existing `src/v3_2/structural.ts` transformations are meta-level dependent
telescope maps used by source scoping. They are not the internal categorical
weakening/exchange/contraction owners and must not be silently reused as if
they were.

## Binder Information Uses Orthogonal Axes

The frontend must not create a distinct binder mode from every
`fapp*_func`, `tapp*_func`, or related owner. Those owners chiefly encode
elimination dimension and packaging.

The design tracks at least these orthogonal axes:

| Axis | Initial values and interpretation |
| --- | --- |
| plicity | explicit or implicit |
| variation capability | functorial, natural/indexed, or object-only |
| polarity | covariant or contravariant, represented through `Op_cat` and the active `z^-` convention where applicable |
| classifier/cell level | object, arrow, transfor, or higher cell; normally inferred |
| dependency | ordinary or displayed/context-indexed; primarily carried by the classifier/context rather than a flat enum |

Object-only is not synonymous with groupoidal. An object-only variable has no
arrow action. A variable ranging in a groupoidal category can still vary
functorially along invertible arrows. Groupoidality is a property of the
classifier, not the meaning of a `:^o` annotation.

Similarly, `fapp1_func`, `tapp0_func`, and `tapp1_func` are full action or
evaluation functors. They are not evidence for three new source binder modes.

The canonical source already settles `:^n` while leaving additional
functorial/object-only source annotations open. USABILITY-1A may choose an
internal TypeScript representation without prematurely standardizing final
Lambdapi notation.

## Reuse From The Older TypeScript Prototype

The `main` worktree's old prototype is implementation evidence, not v3.2
category-theory authority. Useful generic pieces include:

- HOAS-like λ/Π construction;
- binder-mode metadata;
- explicit object/arrow/transfor/displayed action nodes;
- bidirectional elaboration, holes, metavariables, and unification; and
- explicit functor construction with computational coherence checking.

Its mode-aware lambda elaboration nevertheless rebuilt an LF lambda/Π and
validated metadata. It did not compile a functorial lambda automatically to
the active v3.2 categorical structural combinators. The new
bracket-abstraction layer is therefore real new frontend work.

HOAS versus de Bruijn is not the architectural decision. The supported
TypeScript API may remain ergonomic HOAS/PHOAS while immediately replacing
the callback token by a first-order locally nameless node. Stored Core and
transfer artifacts remain immutable and name-independent.

Reusable generic algorithms may be ported or cleanly reimplemented. The old
mutable global rule arrays, stale category-specific AST, named-HOAS storage,
retired D0/D1 compatibility API, and fail-soft behavior must not return.

## Natural And Displayed Staging

The first natural/displayed slice must use only actions already justified by
active owners, initially including:

- `Functord_cat`;
- `Sigma_cat`;
- `Pi_cat`;
- `piapp0` and the applicable `piapp1_*` owners; and
- active displayed functor/transfor projection or internalization owners.

If a candidate needs a missing general displayed contraction, reindexing, or
coherence owner, elaboration fails with a precise
`unsupported-categorical-action` diagnostic. That evidence opens the
smallest focused Lambdapi owner-position proposal under the nested SOP; it
does not authorize inventing a TypeScript-only rule.

This fail-closed boundary lets the compiler architecture stabilize while
mathematical coverage grows incrementally. The later groupoidal
specialization/closure of the existing directed categorical DTT remains a
separate Lambdapi-first development and is not conflated with object-only
frontend variables.

## Implementation Ledger

| Row | Status | Depends on | Deliverable |
| --- | --- | --- | --- |
| USABILITY-PLAN-0 | complete | agreed 2026-07-26 review | This living plan, corrected priority, acquisition deferral, binder architecture, gates, and launch prompt |
| USABILITY-0A | complete | USABILITY-PLAN-0, completed SCALE-STRESS-3B0 infrastructure | Removed only the unused canonical-symbol parser experiment; preserved and validated exact module visibility, tactic-source retention, and 3B closure measurement; repaired the pre-existing runtime fixture to supply its now-required exact provider interface; reconciled navigation and scale ledgers |
| USABILITY-0B | complete | USABILITY-0A, reviewed DIRECTED-1C continuation | Runnable direct-TypeScript dependent demo with readable input, inferred type, serialized explicit Core, reduced result, beta-to-Sigma-fibre trace, and one wrong-family/pair diagnostic |
| USABILITY-1A | pending | USABILITY-0B | Exact binder/application RFC and typed surface judgments; distinguish LF and categorical abstraction, define orthogonal axes, selection rules, contextual IR, provenance, and fail-closed diagnostics against active owners |
| USABILITY-1B | pending | USABILITY-1A | Minimal contextual/wiring IR plus ordinary functorial eta abstraction; object and arrow application select exact `fapp0`/`fapp1_fapp0`; whole-action expectation selects `fapp1_func`; include ambiguity and non-functorial-body negatives |
| USABILITY-1C | pending | USABILITY-1B | Ordinary bracket-abstraction basis: unused-variable/constant weakening, exchange, duplicated-variable diagonal, evaluation after pairing, and nested abstraction/curry; no owner-specific evaluator additions |
| USABILITY-1D | pending | USABILITY-1C | Ergonomic surface/API consolidation, deterministic explicit-Core serialization, stable diagnostics, and focused TypeScript/Lambdapi conformance corpus |
| USABILITY-2A | pending | USABILITY-1D, active displayed owners | One natural/indexed or displayed dependent Pi/Sigma binder and application example through the same contextual IR; precise unsupported-action negative for the first absent structural capability |
| USABILITY-GRADUATE-1 | pending | USABILITY-2A | Architecture review stating the exact mechanically reusable frontend envelope, remaining kernel-owner gaps, residual notation/parser choices, validation, and proposed product/trust boundary |
| SCALE-RETURN-1 | deferred | USABILITY-GRADUATE-1 or independently measured acquisition need | Resume wider direct typed declaration transfer and/or propose a measured narrow acquisition adapter; the current 70-root plus extension closures remain inventory evidence, not a demo prerequisite |

Rows may be split when implementation exposes an independently reusable
mechanism. A failed experiment records the exact classifier, owner, expected
form, and rejection evidence, then opens the smallest correcting row. It does
not permit an owner-named surface/compiler shortcut.

## USABILITY-0A And USABILITY-0B Completion Record

USABILITY-0A preserves the completed SCALE-STRESS-3B0 infrastructure while
removing only the unexported and unconsumed canonical-symbol parser
experiment. The retained generic boundary:

- derives exact immutable public/protected/private interfaces from compiled
  provider modules;
- threads those interfaces through declaration, runtime, proof, and
  source-ordered mixed compilation;
- permits protected dependencies only at their reviewed external runtime
  pattern position;
- retains checked tactic source while installing an opaque theorem
  signature; and
- pins the 58-command protected, 25-command evidence, and 90-command root
  source-prior closures by exact source/export/import/digest evidence.

The strengthened visibility contract exposed one old runtime-composition
fixture that referenced a free dependency module without supplying its
compiled provider interface. The fixture now supplies that exact interface;
the seven existing composition, budget, subject, diamond, dependency,
collision, and immutability behaviors remain unchanged and green. The policy
was not weakened to preserve the old fixture.

USABILITY-0B adds the root-only
`runCoreDirectedDependentDemo()`/`formatCoreDirectedDependentDemo()` API and
the non-interactive command:

```text
./scripts/pnpmw run demo:directed-dependent
```

The implementation uses `createCoreDirectedContinuationKernel()` and
`CoreLfScopedBuilder` directly. Under assumptions
`K : Cat`, `R : Catd K`, a telescope `FF`, `k : Obj K`,
`r : Obj (R k)`, and a section `s`, it builds the outer-LF application:

```text
builder.apply(
  builder.lam("section", sectionType, section =>
    piapp0(sigmaBase, telescopeFamily, section, pair)),
  s
)
```

The command prints the serialized explicit Core, inferred raw fibre type,
reviewed reduced type, and an executable combined fibre witness whose exact
trace is:

```text
1. beta
2. directed.sigma-telescope-fibre.evaluate
```

It also attempts evaluation at a pair whose second component belongs to a
different displayed family `S`. The checker returns a source-located
`TYPE_MISMATCH`; the demo adds the stable user-facing explanation that the
section expects the Sigma telescope over `R`. No string parser, Lambdapi
process, browser export, new owner, runtime rule, proof rule, or semantic
profile participates.

Validation on 2026-07-26:

```text
node --require ts-node/register --test \
  tests/v3_2_directed_dependent_demo_tests.ts \
  tests/v3_2_lf_runtime_fragment_tests.ts \
  tests/v3_2_lf_transfer_visibility_tests.ts \
  tests/v3_2_scale_stress_3b_acquisition_tests.ts
  19 tests / 4 suites: 18 passed, 1 live probe skipped

./scripts/pnpmw run demo:directed-dependent
  passed; printed input, explicit Core, inferred/reduced types,
  two-step trace, and wrong-family diagnostic

./scripts/pnpmw run check:ts
  workspace, typecheck, lint, and 572-test TypeScript gate passed
  534 passed, 38 opt-in live probes skipped

EMDASH_TYPECHECK_TIMEOUT=60s ./scripts/pnpmw run check:scale
  complete TypeScript and kernel/report/book/audit gates passed
  all 19 frozen MVP differential judgments passed
  all 41 bounded Lambdapi kernel/example files passed
  all 11 directed conformance judgments passed
  all 14 live inventory/acquisition checks passed
  all 15 mixed-phase conformance checks passed
  all 5 live proof-stress checks passed
  all 15 live telescope-stress checks passed
  all 14 live profunctor-stress checks passed
  all 8 live module-visibility/acquisition checks passed

python3 emdash2/scripts/lint_report_headers.py
git diff --check
  passed
```

## Acceptance Criteria

USABILITY-GRADUATE-1 may propose that the frontend architecture is settled
only when:

1. direct TypeScript input produces explicit Core without a string parser;
2. the packaged dependent demo infers, checks, evaluates, traces, and rejects
   its negative deterministically;
3. outer LF and categorical abstraction are distinct in the stored IR and
   diagnostics;
4. the ordinary eta, object, arrow, full-action, constant, exchange,
   diagonal, evaluation, and curry cases compile through generic contextual
   rules;
5. adding another ordinary example changes data/typed macros rather than
   checker/evaluator algorithms;
6. one natural/displayed Pi/Sigma example uses the same architecture and
   active owners;
7. a missing displayed structural action fails closed and can be routed to an
   exact Lambdapi owner-position question;
8. TypeScript checking/evaluation and bounded Lambdapi conformance agree for
   all semantically claimed examples;
9. no deployed browser profile, active Lambdapi source, or historical trust
   record changed silently; and
10. the final report distinguishes frontend mechanical reuse, library
    transfer throughput, mathematical owner coverage, notation, and optional
    parsing.

Success does not by itself establish complete groupoidal DTT closure,
whole-development transfer, arbitrary displayed structural logic, general
termination/confluence, standalone TypeScript subject reduction, performance,
release readiness, or a final textual source syntax.

## Human Review Gates

### D-DTTLF-USABILITY-001 — Approved Direction

Approved by the user's 2026-07-26 agreement and implementation instruction:

> Pause and remove the isolated canonical `symbol` parser experiment; retain
> canonical acquisition only as optional future automation. Preserve the
> generic visibility, tactic-source, exact-inventory, transfer, runtime, and
> proof infrastructure. Treat direct typed TypeScript construction as the
> default transfer path. Prioritize a polished existing dependent demo,
> followed by a categorical-binder frontend that distinguishes outer LF
> abstraction from functorial/natural/displayed abstraction, uses
> type-directed application selection and typed categorical bracket
> abstraction, and fails closed on missing active structural owners.

This decision approves the implementation direction, not a new semantic
profile, source notation, parser, Lambdapi rule, product trust claim, or the
final graduation verdict.

### H-DTTLF-USABILITY-02 — New Mathematical Owner

This gate triggers only if USABILITY-2A or an earlier experiment requires a
categorical/displayed structural or coherence owner not currently justified
by the active kernel.

A proposal must name the owner position, source and target, positive
consumer, non-collapse/negative evidence, computation or theorem status,
interaction risks, and whether the gap belongs in Lambdapi mathematics,
surface elaboration, or an existing comparison. No TypeScript workaround may
precede that decision.

### H-DTTLF-USABILITY-GRADUATE — Frontend Architecture

USABILITY-GRADUATE-1 must present:

- the exact implemented surface and contextual IR;
- the application-selection and bracket-abstraction rules;
- the ordinary and natural/displayed witness corpus;
- every unsupported classifier/action;
- TypeScript/Lambdapi differential evidence;
- the mechanical reuse envelope and residual algorithmic risks; and
- an explicit separation between frontend scalability and bulk library
  acquisition.

Human approval is required before calling the final frontend architecture
settled or promoting another product profile.

## Validation And Checkpoint Policy

Focused implementation starts with the smallest affected test files. A
behavioral source change must add or update focused tests and remain wired
into `tests/main_tests.ts` when needed.

The proportional root baseline is:

```text
./scripts/pnpmw run check:ts
EMDASH_TYPECHECK_TIMEOUT=60s make -C emdash2 check
```

The packaged demo adds a deterministic non-interactive command recorded in
`package.json`. Its output is fixture-tested; the command itself is executed
before checkpointing USABILITY-0B.

Any emitted or newly selected categorical owner requires a bounded Lambdapi
positive and relevant negative probe. Changes to continuation semantics also
run `./scripts/pnpmw run check:continuation`; changes to retained scale
mechanisms run their focused lane and `./scripts/pnpmw run check:scale` in
proportion to scope. A substantial cross-layer handoff runs
`./scripts/pnpmw run check:all`.

This plan inherits
[`PERSISTENT_GOAL_GIT_EXPERIMENTATION.md`](./PERSISTENT_GOAL_GIT_EXPERIMENTATION.md).
The user's existing authorization permits local checkpoint commits only on
the existing `goal/typescript-elaborator-v3.2` branch/worktree after a bounded
coherent tranche is green, this ledger/navigation is synchronized, and the
exact staged diff excludes unrelated work.

No push, merge, PR, publication, release, new branch/worktree, rebase, amend,
reset, history rewrite, cleanup, branch deletion, or worktree removal is
authorized.

## Persistent `/goal` Launch Prompt

```text
Kick off or continue implementing
docs/TYPESCRIPT_ELABORATOR_V3_2_USABILITY_PLAN.md.

Treat its Persistent /goal Launch Prompt as part of the objective. Recover
actual state from active code, tests, this plan and its ledgers, the linked
scale/DTT-LF/master plans, all Git worktrees and staged/unstaged diffs, and
the active authority order. Follow the root AGENTS.md and, for every emdash2
action, emdash2/AGENTS.md and the current v3.2 SOP. Resume the in-progress row
or select the next dependency-ready bounded implementation slice; produce
executable evidence rather than only expanding architectural prose.

Preserve the exact deployed emdash-v3.2-mvp-1 profile and exact reviewed
root-only emdash-v3.2-dttlf-directed-1 continuation. Retain the outer
dependent LF, backend-neutral locally nameless explicit Core, scoped
TypeScript builder, immutable catalogs/profiles, generic transfer engines,
small TypeScript checker/evaluator, and mandatory bounded Lambdapi
conformance role.

Follow approved D-DTTLF-USABILITY-001. Keep canonical Lambdapi declaration or
term parsing deferred and optional. Do not recreate the removed canonical
symbol-parser experiment unless a later measured transfer tranche and updated
plan first justify a narrow adapter. Preserve exact inventory/hash evidence,
compiled module visibility, checked tactic-source retention, and the generic
declaration/runtime/proof/mixed-phase transfer mechanisms. Direct typed
TypeScript transfer is the default; “mechanical” means data, linkage, policy,
and focused evidence rather than a new checker/evaluator algorithm.

Preserve completed USABILITY-0A/0B: the parser experiment remains removed,
the generic visibility/tactic/inventory infrastructure remains validated,
and the runnable dependent Sigma-telescope demo retains direct TypeScript
input, inferred and reduced types, explicit Core, the two-step trace, and its
wrong-family diagnostic. No 70-root or extension declaration transfer is a
prerequisite for that demo.

Begin with dependency-ready USABILITY-1A, then specify and implement the
categorical-binder frontend in the ledger's order. Keep outer LF lambda
separate from functorial abstraction. Use a small
typed contextual/wiring IR and lower variable, discard, permutation,
duplication, evaluation, composition, and nested abstraction through active
identity/constant/symmetry/diagonal/product/evaluation/composition/curry
owners. Select explicit fapp/tapp/fdapp/tdapp/piapp eliminators from inferred
classifier, argument dimension, expected type, polarity, and displayed
context. Do not turn every *_func owner into a binder mode; track plicity,
variation capability, polarity, cell level, and displayed dependency as
orthogonal information.

Fail closed with precise provenance whenever an action is ambiguous or an
active displayed structural/coherence owner is missing. Never add an
owner-named AST node, materializer, checker exception, evaluator shortcut, or
TypeScript-only semantic rule merely to pass one example. Any missing
mathematical owner returns to its exact Lambdapi position and the nested SOP.

Use the old main-branch TypeScript prototype only for reusable generic
elaboration, metavariable, unification, explicit-action, and coherence
evidence. Do not restore its stale category theory, mutable global rules,
named-HOAS storage, retired D0/D1 compatibility API, or fail-soft behavior.
An ergonomic HOAS/PHOAS construction API may lower immediately to immutable
first-order locally nameless storage.

Do not call the frontend architecture settled until one ordinary
functorial bracket-abstraction corpus and one natural/displayed dependent
Pi/Sigma example pass end to end and H-DTTLF-USABILITY-GRADUATE approves the
recorded envelope. Keep bulk library-transfer qualification separate; resume
the deferred 70-root/extension closure or propose acquisition automation only
after this critical path or an independently measured need.

Recover the actual descendant HEAD; named baselines and checkpoints are
comparison/backtracking evidence, never permission to reset or rewrite.
Existing authorization permits temporary local checkpoint commits only on
the existing goal branch after a bounded green tranche, synchronized
ledger/navigation, exact staged-diff review, and
git diff --cached --check. It authorizes no push, merge, PR, publication,
release, history rewriting, new branch/worktree, cleanup, or deletion.

Continue safe independent work around a pending human gate. Keep every
Lambdapi process bounded to at most 60 seconds and preserve all proportional
subject-reduction, warning, audit, catalog, health, examples, conformance, and
CI obligations.
```

## Change Log

- **2026-07-26 — USABILITY-PLAN-0 opened and direction approved.** Recorded
  the corrected priority from the user's accepted architecture review:
  preserve the backend and generic transfer work, remove only the isolated
  unused canonical-symbol parser, treat direct typed construction as the
  default, package the already-working dependent demo, and qualify a genuine
  categorical-binder compiler before returning to bulk acquisition.
  Distinguished outer LF lambda from functorial abstraction, recorded
  type-directed application selection, the contextual/wiring IR and ordinary
  bracket-abstraction basis, orthogonal binder axes, old-prototype reuse,
  natural/displayed fail-closed staging, graduation criteria, and the updated
  persistent launch prompt.
- **2026-07-26 — USABILITY-0A consolidation completed.** Removed only the
  isolated canonical-symbol parser experiment and retained the generic exact
  module-visibility, checked tactic-source, transfer-engine, and exact 3B
  closure work. Updated the existing runtime-fragment fixture to provide its
  exact compiled dependency interface rather than weakening visibility.
  Reconciled the master, DTT/LF, scale, handoff, and persistent-goal
  navigation. No active semantic profile or parser trust was added.
- **2026-07-26 — USABILITY-0B dependent demo completed.** Added a root-only
  direct-TypeScript scoped-builder demo and package command. It prints
  explicit Core and inferred/reduced dependent types, executes the exact
  `beta` then `directed.sigma-telescope-fibre.evaluate` trace, and reports a
  readable source-located wrong-family `TYPE_MISMATCH`. Focused, full
  TypeScript, complete scale, all 41 kernel/example, directed, inventory,
  mixed, proof, telescope, profunctor, and module stress gates passed.
  USABILITY-1A is next.
- **2026-07-26 — USABILITY-0A/0B checkpointed.** Recorded exact green local
  checkpoint `c2c6da01386b7fda1d26e71379c4256fb06ea637` after exact staged
  path/diff review and whitespace audit. The checkpoint grants no parser,
  browser/profile expansion, new semantic owner or rule, frontend graduation,
  remote Git operation, integration, publication, release, or cleanup.
