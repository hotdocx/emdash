# D-DTTLF-PRODUCT-BROWSER-001 — Directed Browser Demo Review

Date: 2026-07-29
Gate: H-DTTLF-PRODUCT-BROWSER-01
Decision: D-DTTLF-PRODUCT-BROWSER-001
Status: approved as proposed under the user's standing unattended delegation
Human-Supersession: any later explicit human decision supersedes this record
Reviewed-Proposal-Checkpoint:
`2d0583e` (`docs: plan browser and user syntax continuations`)

## Review

No immediate human objection followed presentation and checkpointing of the
bounded BROWSER-DIRECTED-1A proposal in
[`TYPESCRIPT_ELABORATOR_V3_2_BROWSER_DEMO_PLAN.md`](./TYPESCRIPT_ELABORATOR_V3_2_BROWSER_DEMO_PLAN.md).
The user's standing delegation therefore approves that exact proposal with
human supersession.

The approved implementation may add only:

- an additive browser-safe entry that retains the frozen minimal browser API
  and exposes the existing directed-dependent run/format interface;
- a deeply immutable boundary record for that opt-in continuation;
- a second fixed dependent-LF view in the existing standalone browser
  fixture while preserving its explicit-Core playground;
- focused execution, frozen-boundary, and transitive Node-free tests;
- a strict TypeScript plus Vite production-build command or documented gate;
- one real-browser smoke run; and
- synchronized README, handoff, product, browser, and scale ledgers.

The implementation must execute the actual existing TypeScript checker and
evaluator in the browser. Static copied output does not satisfy the decision.

## Explicit Non-Authorization

This decision authorizes no:

- change to `src/v3_2/browser.ts`, `CORE_MVP_MANIFEST`, or the frozen MVP
  product identity;
- categorical program or ordinary/displayed categorical browser promotion;
- string parser, parser dependency, lockfile change, language server, worker,
  or remote service;
- Node builtin or polyfill in the browser closure;
- production Lambdapi process;
- mathematical owner, runtime/proof/unification rule, checker/evaluator case,
  transfer engine, contextual compiler, or active semantic profile change;
- browser, categorical-usability, scale, release, or whole-transfer
  graduation claim; or
- push, merge, publication, release, PR, rebase, amend, reset, history
  rewrite, cleanup, branch deletion, or worktree removal.

The later runtime-spec/acquisition separation and every categorical browser
entry remain separate gated rows.

## Validation And Git Boundary

Implementation must satisfy the BROWSER-DIRECTED-1A acceptance list, keep the
exact staged diff free of unrelated work, synchronize the living ledgers, and
pass `git diff --cached --check` before an authorized local checkpoint.

This review is a decision record only. It does not itself implement the
browser row.

## Later Human Hosting Clarification

After this review, the user explicitly clarified that Sandpack compatibility
is no longer required and that the likely deployment target is a fully
client-side GitHub Pages site at
`https://hotdocx.github.io/emdash/`, with a Node backend only if static
execution proves infeasible.

That clarification supersedes any Sandpack-oriented packaging assumption and
permits BROWSER-DIRECTED-1A to configure portable relative Vite assets,
remove the obsolete Sandpack-only public HTML fixture, and document static
hosting. It does not authorize creating a GitHub Pages workflow, publishing,
pushing, changing remotes, or adding a backend.
